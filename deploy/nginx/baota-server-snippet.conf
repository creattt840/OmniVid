# 宝塔站点主配置 server { } 内需添加/修改的片段
# 网站 → 配置文件，合并到 server 块中（保留宝塔原有 include 行）

# 在 root 行下方添加：
client_max_body_size 500m;

# 在 include proxy 规则之前添加：
location / {
    try_files $uri $uri/ /index.html;
}

# 建议注释掉（SPA 站点不需要）：
# error_page 404 /404.html;
# include enable-php-00.conf;
